version: "3"

services:
  api:
    container_name: api
    build: ./api
    ports:
      - "6000:6000"
    restart: unless-stopped
  
  etl:
    container_name: etl
    build: ./etl
    depends_on:
      - api
